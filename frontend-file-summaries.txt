{
  "frontend/src/App.tsx": "Main application component with centralized navigation logic that uses a `manualNavRef` flag to prevent the auto-routing `useEffect` from overriding manual navigations (e.g. after registration), ensuring the driver/traveller/admin dashboard is shown immediately after successful registration without being redirected back to the register page.",
  "frontend/src/hooks/useQueries.ts": "Custom React Query hooks for all backend operations; fixes registration mutations to pass the required `name` parameter — `useAdminRegister` now accepts `{ roleText, name }`, `useDriverRegister` accepts `{ name, busNumber }` (matching backend argument order), and `useTravellerRegister` accepts `{ name }`.",
  "frontend/src/pages/HomePage.tsx": "Landing page with hero section, feature cards, and how-it-works guide showcasing the bus tracking system.",
  "frontend/src/pages/RegisterPage.tsx": "Role-based registration page with name input fields added to all three tabs (Admin, Driver, Traveller); each form now collects the user's name and passes it to the corrected mutation hooks, with proper validation and loading states.",
  "frontend/src/pages/LoginPage.tsx": "Login page with Internet Identity authentication; fixes the unclickable UI bug by adding `relative` positioning to the root wrapper div and `pointer-events-none` to the decorative background image overlay.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin control panel with driver/traveller management tables; fixes the unclickable UI bug by adding `relative` to the root wrapper div and `pointer-events-none` to the decorative background overlay.",
  "frontend/src/pages/DriverDashboard.tsx": "Driver interface for bus registration and location sharing toggle; fixes the TypeScript error by updating the in-dashboard bus registration form to collect a driver name field and pass `{ name, busNumber }` to `driverRegister.mutateAsync`, matching the updated hook signature.",
  "frontend/src/pages/TravellerDashboard.tsx": "Traveller dashboard with bus tracking, canvas-based BusMap integration (replacing Google Maps), and proximity alarm; displays real-time bus location using the existing BusMap component with live coordinate updates every 3 seconds.",
  "frontend/src/components/Header.tsx": "Navigation header with authentication controls, user profile dropdown, and role-based navigation using UserRole enum for proper role comparison and dashboard routing.",
  "frontend/src/components/Footer.tsx": "Minimal application footer displaying only the current year and app name copyright notice, with no third-party attribution.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time user profile setup requiring name input, now correctly using UserRole enum from backend types.",
  "frontend/src/components/BusMap.tsx": "Canvas-based map component with smooth animated bus movement, pulse effects, route trails, realistic road visualization, and automatic position interpolation for fluid transitions between location updates.",
  "frontend/src/index.css": "Global styles with OKLCH color system customized for blue/indigo theme with proper light and dark mode support.",
  "frontend/src/utils/backendErrors.ts": "Helper utility that normalizes backend errors (trapped calls, rejected promises, authorization errors) into user-friendly English messages for consistent error handling across the application.",
  "frontend/index.html": "Clean HTML entry point for the app with no Google Maps API key exposure or external map script tags.",
  "frontend/src/utils/haversineDistance.ts": "Utility function that calculates the great-circle distance in metres between two GPS coordinate pairs using the Haversine formula.",
  "frontend/src/lib/useGeolocation.ts": "Custom React hook that wraps the browser Geolocation watchPosition API to continuously track the traveller's device coordinates, returning coords, loading state, error message, and a permissionDenied flag.",
  "frontend/src/lib/useProximityAlarm.ts": "Custom React hook that computes the distance between bus and traveller using the Haversine utility, fires a Web Audio API beep alarm once when the bus enters the 500 m threshold, resets when the bus moves away, and exposes alarm state plus a dismiss handler.",
  "frontend/src/components/ProximityAlertBanner.tsx": "Prominent amber alert banner component that displays a \"Your bus is nearby!\" message with distance info and a dismiss button when the proximity alarm is triggered.",
  "frontend/src/components/GoogleBusMap.tsx": "Google Maps component that dynamically loads the Maps JavaScript API and renders bus and traveller markers with custom icons; all Google Maps object references are typed as `any` to avoid the missing `@types/google.maps` dependency, while the component's own props and state remain fully typed."
}